the project implements the functions of working with groups of strings,
sorting strings, getting strings from a file and writing strings to a file

[-alnum] sorting mode that takes into account only letters and numbers
[-help] print this text
